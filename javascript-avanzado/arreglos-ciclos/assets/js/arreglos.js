/* const numeros =[5]; */
let numeros = new Array();
numeros = [1, 3, 13, 23, 532, 328, 123, 6865, 812, 213, 321];

document.write(numeros[5]);

for (let i = 0; i < numeros.length; i+2 ) {
    document.write(`${numeros[i]}<br>`);
    /* const numero = numero[i];
    document.write(numero + '<br>') */
};


